# Finance V27

- Booking Management now uses a split-screen dispatch workspace instead of the old full-width table.
- The production `app/globals.css` contains the dispatch styles (previous packages only updated `src/app/globals.css`).
- Vehicle Rate automatically adds missing Hourly Disposal and Cross Border rows to existing saved rate tables without deleting existing prices.
- Recommended starter rows:
  - Hourly Disposal (minimum 2 hours)
  - Cross Border SG to JB (from)
