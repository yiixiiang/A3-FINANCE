# Mobile Editing & Cloud Sync Fix (v5.1)

- Cloud synchronization no longer remounts the active module while a form is open.
- Unsaved edits remain on screen during background and manual cloud sync.
- Tapping the dark backdrop on mobile no longer closes an edit form accidentally.
- Modal headers and Save/Cancel buttons remain visible while scrolling.
- Inputs use mobile-safe 16px text to prevent browser zoom.
- Website Catalogue toolbars, tabs and stall controls are responsive on small screens.

Save the record normally. Cloud synchronization continues in the background without forcing the user out of the editor.
