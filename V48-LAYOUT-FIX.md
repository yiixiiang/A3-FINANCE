# V48 Limousine Management Layout Fix

- Prevents the limousine workspace from overflowing or clipping on laptop screens.
- Switches the sidebar to drawer mode on screens up to 1280px for more usable workspace width.
- Makes the three workflow cards responsive.
- Keeps a single Publish All action and prevents it from covering content on smaller screens.
- Makes tabs and wide tables usable on desktop, tablet and mobile.
- Removes conflicting sticky jump navigation behaviour.
