# V51 Production Edition

Adds publish metadata, a visible last-live timestamp, publish duration, and CDN-friendly public snapshot caching. The published API now includes `published_at`, `published_by`, and `publish_version`.
